/** API and client storage keys (edit if you proxy under a subpath). */
export const SESSION_KEY = "persona_chat_session_id";
export const API_CHAT_URL = "/api/chat";

/** Max textarea height in px (must match CSS --composer-max-height roughly). */
export const TEXTAREA_MAX_HEIGHT_PX = 160;
